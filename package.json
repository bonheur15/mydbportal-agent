{
  "name": "my-app",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build":"bun build ./src/index.ts --compile --outfile out/linux-x64 --target=bun-linux-x64"
  },
  "dependencies": {
    "@hono/node-server": "^1.16.0",
    "hono": "^4.8.4"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}